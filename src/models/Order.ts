export type Order = {
    id: number;
    values: Record<string, string | number | boolean>;
};

// export type OrderValue = {
//     isEnabled: boolean;
//     value: string | number | boolean;
// };
