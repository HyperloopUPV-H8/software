import { Measurement } from "common";

interface Props {
    measurements: Measurement[];
}

export const BarIndicatorStack = ({measurements}: Props) => {
    return (
        <div>
            
        </div>
    )
}
